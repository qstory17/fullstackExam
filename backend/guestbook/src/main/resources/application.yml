server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/guestbook?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: root
    password: root1234 # ??? ???? ??
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # ??
    show-sql: true     # ???? SQL ?? ??